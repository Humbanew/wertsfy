name: ESLint Workflow

on: 
  push:
    branches: [ main, version-2.0.0 ]
  pull_request:
    branches: [ main, version-2.0.0 ]

jobs:
  welcome:
    
    steps:
      name: 🎉 Enabled this action.
