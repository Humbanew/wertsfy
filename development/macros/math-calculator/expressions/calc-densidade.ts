const formulaCalculoDaDensidade = (massa: number, volume: number): number => {
  let resultado = massa / volume;
  return resultado;
}
export { formulaCalculoDaDensidade as Macro_math_calculator_exp_calc_densidade_method }
