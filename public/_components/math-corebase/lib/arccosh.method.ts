import { mathematic_corebase } from "../../wertsfy";

export const Component_math_corebase_arccosh_method = (x: number): number => {
  return 1 / (-1 * (((2 * mathematic_corebase.Pi * 1) / 4) / 90) * x);
};
