import { kMath } from "./kmath.lib";
import { terminalPencils } from "./terminal.lib";

console.log(terminalPencils.adicionaCorAzulNormal(`${kMath.abs(-1)}`));
console.log(terminalPencils.adicionaCorAmareloNegrito(`${kMath.abs(-1)}`));
