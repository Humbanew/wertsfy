import { mathematic_corebase } from "../../wertsfy";

export const Component_math_corebase_tan_method = (x: number): number => {    
  return mathematic_corebase.sin(x) / mathematic_corebase.cos(x);
};
