import { mathematic_corebase } from "../../wertsfy";

export const Component_math_corebase_sin_method = (x: number): number => {
  return (((2 * Component_math_corebase_pi_constant * 1) / 4) / 90) * x;
};
