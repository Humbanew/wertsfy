const formulaCalculoDaFrequencia = (periodo: number): number => {
  let resultado = 1 / periodo;
  return resultado;
}
export { formulaCalculoDaFrequencia as Macro_math_calculator_exp_calc_frequencia_method }
