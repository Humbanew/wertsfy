/*
 *   Copyright (c) 2020 
 *   All rights reserved.
 */

class Visitador{

  verificador(valores=[0]){
    let valor = "arcos_trigonometricos";
    let regex = /([a-z]+)(_)?/g;
    var resultado = null;

    while (resultado = regex.exec(valor)){
      // resultado;
      regex.lastIndex;
    }
  }
}